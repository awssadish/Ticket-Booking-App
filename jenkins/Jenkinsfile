pipeline {
    agent any

    environment {
        DOCKERHUB_USER = "sadishdocker"
        BACKEND_IMAGE  = "${DOCKERHUB_USER}/app-backend"
        FRONTEND_IMAGE = "${DOCKERHUB_USER}/app-frontend"
        IMAGE_TAG      = "${BUILD_NUMBER}"
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/sadish2006/Three-tier-application-Resume.git'
            }
        }

        stage('Backend Unit Tests') {
            steps {
                dir('app/backend') {
                    sh '''
                      set -e
                      npm ci
                      npm test || echo "No tests found, skipping"
                    '''
                }
            }
        }

        stage('Frontend Unit Tests') {
            steps {
                dir('app/frontend') {
                    sh '''
                      set -e
                      npm ci
                      npm test || echo "No tests found, skipping"
                    '''
                }
            }
        }

        stage('Build Docker Images') {
            steps {
                sh """
                  docker build -t ${BACKEND_IMAGE}:${IMAGE_TAG} app/backend
                  docker build -t ${FRONTEND_IMAGE}:${IMAGE_TAG} app/frontend
                """
            }
        }

        stage('Tag & Push Images to Docker Hub') {
            steps {
                withDockerRegistry(
                    credentialsId: 'docker-creds',
                    url: 'https://index.docker.io/v1/'
                ) {
                    sh """
                      set -e

                      docker tag ${BACKEND_IMAGE}:${IMAGE_TAG} ${BACKEND_IMAGE}:latest
                      docker tag ${FRONTEND_IMAGE}:${IMAGE_TAG} ${FRONTEND_IMAGE}:latest

                      docker push ${BACKEND_IMAGE}:${IMAGE_TAG}
                      docker push ${BACKEND_IMAGE}:latest

                      docker push ${FRONTEND_IMAGE}:${IMAGE_TAG}
                      docker push ${FRONTEND_IMAGE}:latest
                    """
                }
            }
        }
    }

    post {
        success {
            echo "✅ CI pipeline completed successfully"
        }
        failure {
            echo "❌ CI pipeline failed"
        }
    }

