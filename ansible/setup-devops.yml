---
- name: Setup Jenkins, Docker, kubectl, Minikube
  hosts: jenkins
  become: yes
  roles:
    - role: ../roles/jenkins
    - role: ../roles/docker
    - role: ../roles/minikube
    - role: ../roles/kubectl
    
    
