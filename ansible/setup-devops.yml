---
- name: Setup Jenkins, Docker, kubectl, Minikube
  hosts: jenkins
  become: yes
  roles:
    - role: ../roles/jenkins
    - role: ../roles/docker
    - role: ../roles/kubectl
    - role: ../roles/minikube
    
    
